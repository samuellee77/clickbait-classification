# TODO

1. Hyperparameters tuning (tuning with only 10k data for faster training)
2. try different models (BERT DistilBERT, RoBERTa)
3. Compare with baseline (TF-IDF with Logisitic Regression and Random Forest)
